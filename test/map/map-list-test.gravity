#include "./src/lo.gravity"

#unittest {
  name: "Lo.map list";
  error: NONE;
  result: '[2,3,4,5,6]';
};

func main() {
  var list = [1,2,3,4,5];

  return Lo.map(list, func(number) {
    return number + 1;
  }).String();
}
